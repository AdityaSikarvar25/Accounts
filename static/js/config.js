// API base URL.
// Leave empty when Flask serves the frontend (default Render deployment).
// Change to your Render URL only if using Netlify + Render split: 'https://your-app.onrender.com'
const API_BASE = '';
